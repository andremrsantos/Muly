<%= form_for(@faixa) do |f| %>
  <% if @faixa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@faixa.errors.count, "error") %> prohibited this faixa from being saved:</h2>

      <ul>
      <% @faixa.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titulo %><br />
    <%= f.text_field :titulo %>
  </div>
  <div class="field">
    <%= f.label :letra %><br />
    <%= f.text_area :letra %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
